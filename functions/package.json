{
  "name": "voicedeutschmaster-functions",
  "description": "Firebase Functions for VoiceDeutsch Master",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-functions": "^6.0.0",
    "@google/genai": "^1.0.0"
  },
  "private": true
}